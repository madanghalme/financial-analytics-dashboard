services:
  mongo:
    image: mongo:7
    container_name: financial-analytics-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - financial_analytics_mongo:/data/db

volumes:
  financial_analytics_mongo:
