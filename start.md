Quick start:
1. docker compose up -d mongo
2. cd backend && cp .env.example .env && npm install && npm run seed && npm run dev
3. New terminal: cd frontend && cp .env.example .env && npm install && npm run dev
4. Open the Vite URL and sign in with analyst@example.com / Password@123
